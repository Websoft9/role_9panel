php_runtime_meta:
  apache:
    vhost_dir: /etc/httpd/conf.d
    vhost_src: vhost.conf
  nginx:
    vhost_dir: /etc/nginx/conf.d
    vhost_src: default.conf
  caddy:
    vhost_dir: /etc/caddy
    vhost_src: caddyfile.conf
